# Sovereign Guard Secrets
.env.sovereign
guard_monitor.log
guard_watchdog.out
guard_watchdog.err
guard_supervisor.pid
developer_mode.lock

# Virtual Env
venv/
__pycache__/
*.pyc

# Proprietary Core Logic (Encrypted via git-crypt)
# sovereign_core.py is NO LONGER ignored, it is tracked but encrypted.

# Encryption Keys (NEVER COMMIT)
*.key
.env.sovereign
